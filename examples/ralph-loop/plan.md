# Plan

One checklist line per task. The ralph coder implements the FIRST `- [ ]` item
each pass and ticks it `- [x]`; impossible tasks become `- [!]` with an
indented reason. The adversarial reviewer appends its findings as new `- [ ]`
lines at the end. Replace the demo tasks below with your own.

- [ ] Create a `greet` package with `Greet(name string) string` returning "Hello, <name>!" and defaulting to "Hello, world!" for an empty name, with table-driven tests
- [ ] Add a `cmd/greet` CLI that prints `Greet` of its first argument and exits 0
- [ ] Document the greet CLI usage in the project README
